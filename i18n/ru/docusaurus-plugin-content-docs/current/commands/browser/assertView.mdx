import Admonition from "@theme/Admonition";
import AssertViewOptions from "../../_partials/specs/assert-view-options.mdx";
import AssertViewHistory from "../../_partials/specs/assert-view-history.mdx";

# assertView

## Обзор {/* #overview */}

Используйте команду `assertView`, чтобы снять скриншот для определенного состояния теста и сравнить его с эталонным.

<Admonition type="info">
    Команда `assertView` использует [waitForExist][wait-for-exist] (с
    [waitTimeout][wait-timeout] и [waitInterval][wait-interval]), чтобы дождаться появления элемента перед снятием скриншота.

    Также она автоматически ждет загрузки статики (шрифтов, изображений и т.д.) с помощью команды [waitForStaticToLoad][wait-for-static-to-load].

</Admonition>

## Использование {/* #usage */}

```typescript
await browser.assertView(state, options);
await browser.assertView(state, selector, options);
```

## Параметры команды {/* #parameters */}

<table>
    <thead>
        <tr>
            <td>**Имя**</td>
            <td>**Тип**</td>
            <td>**Описание**</td>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>[state](#state)</td>
            <td>`string`</td>
            <td>
                Обязательный параметр. Название состояния теста. Должно быть уникальным в пределах
                одного теста.
            </td>
        </tr>
        <tr>
            <td>[selector](#selector)</td>
            <td>`string | string[]`</td>
            <td>
                Необязательный параметр. Может быть пропущен. Селектор DOM-элемента, который нужно
                снять. Если параметр не передан, снимается текущий вьюпорт.
            </td>
        </tr>
        <tr>
            <td>[options](#options)</td>
            <td>`AssertViewOpts`</td>
            <td>Необязательные настройки команды _assertView_.</td>
        </tr>
    </tbody>
</table>

### state

Обязательный параметр. Задает название состояния теста. Название должно быть уникальным в пределах одного теста.

### selector

Необязательный параметр. Задает селектор DOM-элемента, который нужно снять:

-   Если селектор не указан, `assertView` снимет текущий вьюпорт

-   Если передан массив селекторов, `assertView` снимет область, которая покрывает все переданные селекторы

-   Если какой-либо селектор нашел несколько элементов, `assertView` снимет первый из них

### options

Задает настройки команды `assertView`:

<AssertViewOptions />

## Примеры использования {/* #examples */}

**Визуальная проверка элемента**

```typescript
it("should assert view", async ({ browser }) => {
    await browser.url("some/url");
    await browser.assertView("plain", ".button");

    await browser.click(".button");
    await browser.assertView("clicked", ".button");
});
```

**Визуальная проверка всего вьюпорта**

```typescript
it("should assert viewport without selector", async ({ browser }) => {
    await browser.url("some/url");
    await browser.execute(() => window.scrollTo(0, 1000));
    await browser.assertView("plain");

    await browser.$(".button").click();
    await browser.assertView("clicked", { ignoreDiffPixelCount: 5 });
});
```

**Визуальная проверка с дополнительными опциями**

```typescript
it("should assert view with given options", async ({ browser }) => {
    await browser.url("some/url");
    await browser.assertView("plain", ".form", {
        ignoreElements: [".link"],
        tolerance: 5,
        antialiasingTolerance: 4,
        allowViewportOverflow: true,
        captureElementFromTop: true,
        compositeImage: true,
        screenshotDelay: 10,
        selectorToScroll: ".modal",
    });
});
```

## История

<AssertViewHistory />

## Связанные команды {/* #related */}

-   [element.assertView](../../element/assertView)
-   [browser.saveRecordingScreen](../saveRecordingScreen)
-   [browser.saveScreenshot](../saveScreenshot)
-   [element.saveScreenshot](../../element/saveScreenshot)

[browsers-tolerance]: ../../../reference/config/browsers#tolerance
[browsers-antialiasing-tolerance]: ../../../reference/config/browsers#antialiasing_tolerance
[wait-for-static-to-load]: ../waitForStaticToLoad
[wait-for-exist]: ../../element/waitForExist
[wait-timeout]: ../../../reference/config/browsers#wait_timeout
[wait-interval]: ../../../reference/config/browsers#wait_interval
